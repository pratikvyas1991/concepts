<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.concepts.myconcepts">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <uses-library
            android:name="com.google.android.maps"
            android:required="true" />

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_api_key" />


        <activity android:name=".usecases.usercrud.UserSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".usecases.usercrud.UserRegistration"/>
        <activity android:name=".usecases.usercrud.UserLogin"/>

        <activity android:name=".mapslocations.Splash"/>

        <activity android:name=".mapslocations.MapsACtivityHome"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <activity android:name=".reactiveprogramming.CompositeDisposable"/>
        <activity android:name=".customviews.CircularImageView"/>
        <activity android:name=".encryption.EncrypttionActivity"/>
        <activity android:name=".validations.ValidationCheckerActivity"/>
        <activity android:name=".reactiveprogramming.SampleBasicIntro"/>
        <activity android:name=".reactiveprogramming.SampleDisposable"/>
        <activity android:name=".reactiveprogramming.SampleOperator"/>
    </application>

</manifest>